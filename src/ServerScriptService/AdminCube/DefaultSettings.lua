-- Admin Cube - Default Settings

return {
    Groups = {

    };
    Owners = { -- 4
    
    };
    Admins = { -- 3

    };
    Mods = { -- 2

    };
    Vips = { -- 1

    };
    Players = { -- 0

    };
    Banned = {

    };

    Prefix = "!";
    DataStoreKey = "AdminCube-DataStore-1"; -- If Changed All Data is Reset
    DisplayNames = true; -- Display Names Enabled / Disabled for Commands
    TempPerms = false;

    Client = {
        KeyboardNavDetour = true; -- This sets GuiService.AutoSelectGuiEnabled to false, and enables a custom xbox/keyboard naviagation unless disabled below.
        KeyboardNavEnabled = true; -- Custom Keyboard Navigation enabled
        XboxNavEnabled = true; -- Custom Controller Navigation enabled
    }
}
